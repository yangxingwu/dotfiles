name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin
            ~/go/bin
          key: macos-tools-bin-v1
      - name: Run bootstrap
        run: ./bootstrap-macos.sh
      - name: Add brew bash to PATH
        run: echo "$(brew --prefix)/bin" >> "$GITHUB_PATH"
      - name: Run integration tests
        run: bash tests/test_install.sh

  test-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/cache@v5
        with:
          path: |
            /tmp/cargo-cache
            /tmp/go-cache
          key: ubuntu-tools-bin-v1
      - name: Prepare cache dirs
        run: mkdir -p /tmp/cargo-cache /tmp/go-cache && chmod -R 777 /tmp/cargo-cache /tmp/go-cache
      - name: Build test image
        run: docker build -f tests/Dockerfile.ubuntu -t dotfiles-test-ubuntu .
      - name: Run integration tests
        run: docker run --rm -v /tmp/cargo-cache:/home/testuser/.cargo -v /tmp/go-cache:/home/testuser/go dotfiles-test-ubuntu
      - name: Fix cache permissions
        if: always()
        run: sudo chmod -R a+r /tmp/cargo-cache /tmp/go-cache

  test-fedora:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/cache@v5
        with:
          path: |
            /tmp/cargo-cache
            /tmp/go-cache
          key: fedora-tools-bin-v1
      - name: Prepare cache dirs
        run: mkdir -p /tmp/cargo-cache /tmp/go-cache && chmod -R 777 /tmp/cargo-cache /tmp/go-cache
      - name: Build test image
        run: docker build -f tests/Dockerfile.fedora -t dotfiles-test-fedora .
      - name: Run integration tests
        run: docker run --rm -v /tmp/cargo-cache:/home/testuser/.cargo -v /tmp/go-cache:/home/testuser/go dotfiles-test-fedora
      - name: Fix cache permissions
        if: always()
        run: sudo chmod -R a+r /tmp/cargo-cache /tmp/go-cache
